<% include ./partials/header %>
<h1>INDEX PAGE!!!</h1>

<% blogs.forEach(blog => { %>
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>">
    <p><%= blog.body %></p>
    <p><%= blog.created %></p>
<% }) %>
<% include ./partials/footer %>